# KotaJa — Consulta pública de cotações de veículos

Sistema para captura e consulta de preços de veículos, com backoffice por papéis
(admin, gerente, coordenação, lojista e pesquisador) e consulta pública sem login.

## Rodar localmente

### 1) Criar venv e instalar dependências
```bash
python -m venv .venv
# Windows PowerShell
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
